- a simple chat app made using Express.js and Socket.io while using plain HTML/CSS and vanilla JavaScript for the frontend.

  ## Usage
  - clone repository 
```
npm install
npm run dev

run on localhost:3000
```
